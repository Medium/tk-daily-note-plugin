# Memory note template

Create with `create_document`: title `Daily Note Memory`, tag `daily-note-memory`, personal doc (no library — it's private steering state, not shared content). Body:

```markdown
This note steers the daily-note skill. Edit anything here — Claude reads it before every run. Claude also appends to Learned rules when you leave durable instructions ("always…", "from now on…") in a Daily Note.

## Preferences

- Timezone: *(on first run, infer from the machine's local time and write it here)*
- Work hours: 9am-6pm weekdays
- Weather locations: *(add zip codes or city names)*
- News interests: top general/tech stories; anything relevant to Medium strategy (AI, search, publishing)
- Work context: works at Medium *(refine: role, teams, what counts as "needs my attention")*
- Linear scope: teams and projects I belong to
- Sections: default (Notes to Claude, TODO, Weather, News, Calendar, Linear, Pre-Briefings)

## Learned rules

*(Claude appends dated rules here from your Notes-to-Claude instructions.)*
```
